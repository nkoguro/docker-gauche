Vanilla Gauche for Docker
=========================
Dockerfiles for Gauche stable and HEAD.

How to build
============
    % ./make_stable.sh
    % ./make_head.sh
    % ./make_alpine.sh # To build Gauche on Alpine linux.
